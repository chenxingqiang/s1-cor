# Chain of Reward (CoR) 理论框架

## 1. 核心思想

### 1.1 从终极奖励到奖励链

CoR的核心思想：**不对最终输出给予单一的总结性奖励，而是将奖励信号沿着模型生成的思考链进行分布，形成与思考步骤对应的"奖励链"。**

| 范式 | 流程 |
|------|------|
| **传统RLMT** | `Prompt → [思考链 CoT] → 最终回答 → 最终奖励` |
| **CoR** | `Prompt → [思考步骤₁ → 奖励₁] → [思考步骤₂ → 奖励₂] → ... → [最终回答 → 最终奖励]` |

### 1.2 CoR vs 传统RLMT

| 特性 | 传统RLMT | CoR（奖励链） |
|------|----------|---------------|
| 奖励粒度 | 粗粒度（最终输出） | 细粒度（推理过程链） |
| 训练信号 | 稀疏 | 密集 |
| 优化焦点 | 答案质量 | 推理路径质量 |
| 类比 | 只看考试最终分数 | 关注解题的每一步思路 |

---

## 2. 数学建模

### 2.1 符号定义

| 符号 | 定义 |
|------|------|
| $\mathcal{S}$ | 状态空间：模型推理过程中的中间状态 |
| $\mathcal{A}$ | 动作空间：模型生成的每个推理步骤（token） |
| $\pi_\theta(a\|s)$ | 策略：在状态 $s$ 下选择动作 $a$ 的概率 |
| $\tau$ | 思考链轨迹：$\tau = \{(s_0,a_0), ..., (s_T,a_T)\}$ |
| $c$ | 完整输出：$c = \langle\text{think}\rangle y_{\text{think}} \langle\text{answer}\rangle y_{\text{answer}}$ |

### 2.2 奖励分解

**总奖励函数**（四分量分解）：

$$R(c) = R_{\text{ext}}(c) + \lambda R_{\text{int}}(c) + \mu R_{\text{improve}}(c) + \nu R_{\text{converge}}(c)$$

| 分量 | 定义 | 作用 |
|------|------|------|
| $R_{\text{ext}}(c)$ | 外部奖励：$\mathbb{1}[y_{\text{answer}} = y_{\text{gt}}]$ | 最终答案正确性（稀疏） |
| $R_{\text{int}}(c)$ | 内生奖励：$\sum_{d=1}^{D} w_d r_d(y_{\text{think}}) + w_{\text{self}} r_{\text{self}}$ | 推理过程质量（密集） |
| $R_{\text{improve}}(c)$ | 改进奖励：$Q(c_{k+1}) - Q(c_k)$ | 迭代反思的质量提升 |
| $R_{\text{converge}}(c)$ | 收敛奖励：$\exp(-\alpha \|c_K - c_{K-1}\|)$ | 解决方案稳定性 |

### 2.3 奖励链形式化

$$\text{CoR}(\tau) = \sum_{t=0}^{T} \gamma^t r_{\text{int}}(s_t, a_t, s_{t+1})$$

其中 $\gamma \in [0,1]$ 是折扣因子。

---

## 3. 内生自评估机制

### 3.1 多维度评分

模型在推理过程中生成自评估：

```
[Self-Rating: Consistency=8/10, Completeness=9/10, Accuracy=7/10, Clarity=8/10, Format=9/10]
```

**五个评估维度**：

| 维度 | 符号 | 评估内容 |
|------|------|----------|
| 一致性 | $r_{\text{consistency}}$ | 逻辑连贯性，步骤间无矛盾 |
| 完整性 | $r_{\text{completeness}}$ | 推理步骤完整，无遗漏 |
| 准确性 | $r_{\text{accuracy}}$ | 事实正确性，计算无误 |
| 清晰性 | $r_{\text{clarity}}$ | 表达清晰，易于理解 |
| 格式 | $r_{\text{format}}$ | 结构规范，自评格式正确 |

### 3.2 自评估校准

$$r_{\text{self}} = \frac{1}{D} \sum_{d=1}^{D} \text{cal}_d\left(\frac{s_d}{10}, q_d\right)$$

校准函数：
$$\text{cal}_d(u, v) = 1 - |u - v|, \quad u, v \in [0,1]$$

**核心洞察**：模型不仅要"思考得好"，还要"准确评估自己思考的质量"，形成元认知学习信号。

---

## 4. GRPO优化框架

### 4.1 算法流程

对于每个输入 $x$：

1. **采样**：生成 $N$ 个候选思考链 $\{c^{(1)}, ..., c^{(N)}\}$
2. **奖励计算**：对每个 $c^{(i)}$ 计算 $R(c^{(i)})$
3. **组统计**：计算均值 $\mu_R$ 和标准差 $\sigma_R$
4. **优势归一化**：$A^{(i)} = \frac{R(c^{(i)}) - \mu_R}{\sigma_R + \varepsilon}$

### 4.2 目标函数

$$J(\theta) = \mathbb{E}_{x} \left[ \frac{1}{N} \sum_{i=1}^{N} \min\left( r_i A^{(i)}, \text{clip}(r_i, 1-\delta, 1+\delta) A^{(i)} \right) \right] - \beta D_{\text{KL}}(\pi_\theta \| \pi_{\text{ref}})$$

其中：
- $r_i = \frac{\pi_\theta(c^{(i)} | x)}{\pi_{\theta_{\text{old}}}(c^{(i)} | x)}$ 是重要性采样比率
- $\delta$ 是裁剪范围
- $\beta$ 控制KL正则化强度

### 4.3 优势分解

可选的优势分解：

$$A^{(i)}_{\text{total}} = A^{(i)}_{\text{ext}} + \lambda A^{(i)}_{\text{int}}$$

其中：
$$A^{(i)}_{\text{ext}} = \frac{R_{\text{ext}}(c^{(i)}) - \mu_{R_{\text{ext}}}}{\sigma_{R_{\text{ext}}} + \varepsilon}$$
$$A^{(i)}_{\text{int}} = \frac{R_{\text{int}}(c^{(i)}) - \mu_{R_{\text{int}}}}{\sigma_{R_{\text{int}}} + \varepsilon}$$

---

## 5. 双耦合演化动力学

### 5.1 耦合方程

设 $\mathbf{r}_t = (R_{\text{ext}}, R_{\text{int}}, R_{\text{improve}}, R_{\text{converge}})_t$ 为奖励向量，$\theta_t$ 为策略参数：

$$\theta_{t+1} = \theta_t + \alpha \nabla_\theta J(\theta_t; \mathbf{r}_t) \quad \text{(GRPO更新)}$$

$$\mathbf{r}_{t+1} = \mathcal{R}(\pi_{\theta_{t+1}}) \quad \text{(CoR评估)}$$

### 5.2 协同反馈循环

```
┌─────────────────────────────────────────────────┐
│                                                 │
│   更好的策略 → 更高质量的推理链 → 更强的奖励信号 │
│        ↑                                    │   │
│        └────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 6. 理论保证

### 6.1 贝尔曼方程扩展

$$Q^*(s,a) = \mathbb{E}_{s'} \left[ r_{\text{int}}(s,a) + \lambda r_{\text{ext}}(s,a) + \gamma \max_{a'} Q^*(s',a') \right]$$

### 6.2 势函数奖励塑形

定义势函数：
$$\phi(s) = \sum_{k=1}^{K} w_k \cdot f_k(s)$$

内生奖励：
$$r_{\text{int}}(s,a) = \gamma \Phi(s') - \Phi(s)$$

**定理（最优策略保持）**：基于势函数的奖励塑形不改变最优策略集。

### 6.3 收敛性定理

**定理（Lyapunov稳定性）**：定义Lyapunov函数
$$V(\theta, \mathbf{r}) = -J(\theta; \mathbf{r}) + \gamma \|\mathbf{r} - \mathbf{r}^*\|^2$$

在适当条件下：
$$V(\theta_{t+1}, \mathbf{r}_{t+1}) \leq V(\theta_t, \mathbf{r}_t) - \epsilon \|\nabla V\|^2$$

保证耦合系统收敛到稳定平衡点。

### 6.4 迭代改进定理

**定理（反思单调改进）**：设 $\{c_1, ..., c_K\}$ 为反思链序列，若精炼算子 $T$ 满足收缩性质：
$$\|Q(T(c)) - Q^*\| \leq \rho \|Q(c) - Q^*\|, \quad \rho < 1$$

则累积改进奖励有下界：
$$\sum_{k=1}^{K-1} R_{\text{improve}}(c_k, c_{k+1}) \geq (1 - \rho^{K-1})(Q^* - Q(c_1))$$

---

## 7. 实现路径

### 7.1 三阶段训练管道

| 阶段 | 方法 | 目标 |
|------|------|------|
| **阶段1：冷启动** | SFT监督微调 | 学习自评估格式和推理模式 |
| **阶段2：拒绝采样** | 正确答案过滤 | 强化正确的思考模式 |
| **阶段3：GRPO强化** | 多候选比较优化 | 探索和优化思考路径 |

### 7.2 超参数配置

| 参数 | 符号 | 推荐值 | 说明 |
|------|------|--------|------|
| 内生奖励权重 | $\lambda$ | 1.0 | 平衡外部和内生奖励 |
| 改进奖励权重 | $\mu$ | 0.5 | 激励迭代改进 |
| 收敛奖励权重 | $\nu$ | 0.1 | 鼓励解决方案稳定 |
| 候选数量 | $N$ | 8 | GRPO组大小 |
| KL惩罚系数 | $\beta$ | 0.01 | 防止策略漂移 |
| 裁剪范围 | $\delta$ | 0.2 | 信任区域约束 |
| 最大反思轮数 | $K$ | 3 | 反思迭代次数 |

---

## 8. CoR如何优化思考质量

### 8.1 激励机制

| 思考模式 | CoR激励方式 |
|----------|-------------|
| **约束枚举** | 完成"明确约束"子目标时给予即时奖励 |
| **主题分组** | 序列价值模型识别分类整理的贡献，给予高价值增量奖励 |
| **迭代优化** | 差分奖励告诉模型自我纠正行为极具价值 |
| **自我反思** | 准确的自评估获得校准奖励 |

### 8.2 一致性约束

确保推理过程与最终决策一致：

$$\mathcal{L}_{\text{cons}} = \mathbb{E}_{\tau} \left[ \left( \text{sign}\left(\sum_{d} w_d f_d(\tau)\right) - \text{sign}(R_{\text{ext}}(\tau)) \right)^2 \right]$$

---

## 9. 理论贡献

### 9.1 形式化创新

1. **统一奖励链框架**：将外部奖励和内生奖励在同一优化框架中形式化
2. **势函数内生奖励**：为推理过程质量提供数学度量
3. **双耦合演化动力学**：CoR与GRPO的协同优化理论
4. **收敛性保证**：Lyapunov稳定性分析

### 9.2 与现有理论的联系

**与强化学习的联系**：
- 多时间尺度奖励信号（密集内生 + 稀疏外部）
- 基于推理质量的势能函数设计
- 组相对策略优化的理论保证

**与CoT推理的联系**：
- 推理过程质量的量化度量
- 推理步骤优化的数学框架
- 显式推理向隐式推理迁移的理论解释

---

## 10. 关键结论

> **CoR将优化目标从"生成一个好的答案"推进到"如何生成一个好答案"的整个过程。通过为思考链本身提供密集、结构化的奖励信号，CoR有潜力系统地培养出更可靠、更高效、更接近人类思维模式的推理能力。**

核心价值：
1. **内生性思考奖励**：模型学会自我评估和自我改进
2. **密集反馈信号**：每个推理步骤都获得质量评估
3. **元认知学习**：训练模型理解"什么是好的思考"
4. **理论保证**：收敛性和单调改进的数学证明
